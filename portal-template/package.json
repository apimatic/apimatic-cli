{
  "name": "apimatic-portal",
  "private": true,
  "type": "module",
  "description": "Build project for the API documentation portal generated by the APIMatic CLI. Dependencies are provided by the CLI at build time.",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev"
  }
}
